\ProvidesPackage{preto}[2026/02/25 Pacote de Estilo Customizado do Prof. Preto]

% ==========================================
% 1. PACOTES NECESSÁRIOS
% ==========================================
\RequirePackage{tikz}
\RequirePackage{pgf}
\RequirePackage{bm}
\RequirePackage{amsmath, amssymb}
\RequirePackage{graphicx}
\RequirePackage{color}
\RequirePackage{soul}
\RequirePackage{tcolorbox}
\RequirePackage[normalem]{ulem}
\RequirePackage{listings}
\RequirePackage{ragged2e}
\RequirePackage{hyperref}

% ==========================================
% 2. TEMA VISUAL
% ==========================================
\usetheme{Warsaw}

% ==========================================
% 3. CONFIGURAÇÃO DE CÓDIGO PYTHON
% ==========================================
\lstset{
  language=Python,
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{blue},
  commentstyle=\color{green},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny,
  frame=single,
  breaklines=true, % Quebra de linha automática
  breakatwhitespace=true, % Quebra de linha em espaços
  showstringspaces=false
}

% ==========================================
% 4. VARIÁVEIS DO RODAPÉ
% ==========================================
\providecommand{\shortauthorname}{Santiago, PRP}
\providecommand{\shorttitletext}{Visão Computacional no Esporte}

% ==========================================
% 5. RODAPÉ COM 3 DIVISÕES + LOGO NO PRIMEIRO PLANO
% ==========================================
\setbeamertemplate{footline}{
  \leavevmode%
  \hbox{%
    \begin{beamercolorbox}[wd=.333333\paperwidth,ht=2.25ex,dp=1ex,center]{author in head/foot}%
      \usebeamerfont{author in head/foot}\shortauthorname
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.333333\paperwidth,ht=2.25ex,dp=1ex,center]{title in head/foot}%
      \usebeamerfont{title in head/foot}\shorttitletext
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.333333\paperwidth,ht=2.25ex,dp=1ex,right]{date in head/foot}%
      \usebeamerfont{date in head/foot}
      \href{https://github.com/paulopreto}{\includegraphics[height=1.5ex]{GitHub-Logo.png} MACMH}\hspace*{1em}
      \insertframenumber{} / \inserttotalframenumber\hspace*{2ex}
    \end{beamercolorbox}
  }
  \vskip0pt%
  % --- DESENHA A LOGO POR ÚLTIMO (GARANTE O PRIMEIRO PLANO) ---
  \begin{tikzpicture}[overlay, remember picture]
    \node[anchor=north east, xshift=-0.0cm, yshift=-0.0cm] at (current page.north east) {\includegraphics[height=1.7cm]{logo_labiocom.png}};
  \end{tikzpicture}
}